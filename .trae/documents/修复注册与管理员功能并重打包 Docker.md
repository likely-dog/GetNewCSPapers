## 目标
1. 修复用户注册导致的 500 错误，注册仅填写邮箱和密码，不再填写兴趣方向。
2. 用户登录后在页面以点击多选方式选择兴趣方向（不超过 5 个），而非文字输入。
3. 管理员可查看所有用户邮箱，并为每个用户单独设置推送时间与每日推送篇数。
4. 管理员界面按“每个领域每天显示 10 篇论文”，排序逻辑维持“先 CCF 等级，再发表时间越早”。
5. 更新容器镜像，确保在 Windows 11 下以 Docker 正常运行。

## 后端变更
- 调整注册接口：移除 interests 字段，注册仅保存 email、password_hash、role=user；返回登录页。
- 修复 500 根因：对旧 SQLite 的 preferences 表做启动时自动迁移（补充 interval_days/last_push_date）以避免列缺失异常；注册后首页读取偏好不再报错。
- 用户兴趣选择：新增或改造 /me 与 /me/interests 接口，支持接收勾选的领域列表，限制最多 5 个。
- 管理员个性设置：新增 user_settings（user_id, push_time, max_per_day）或在 users 表增加 push_time、max_per_day 两列；提供接口读写；调度与手动推送时优先使用个性设置，缺省落到全局设置。
- 论文分组与排序：提供按领域分组候选的查询方法，保持排序规则（CCF 等级 A/B/C → 时间越早），每组截取 10。
- 调度线程：按分钟轮询，逐用户使用其个性推送时间与篇数执行邮件推送；保留管理员一键触发入口。

## 前端（模板）
- 注册页：去掉兴趣输入，仅邮箱/密码；错误信息与引导登录。
- 登录后“我的兴趣”：从 ccf_taxonomy.yaml 加载领域清单，渲染为可点击多选（如 checkbox/tag），保存到后端。
- 管理员页：
  - 用户列表：显示邮箱、兴趣、个性推送时间与篇数，支持逐用户保存。
  - 每日推送预览：分领域展示，每个领域 10 篇（由后端分组排序后提供）。

## 数据库
- 自动迁移 preferences 缺失列。
- 新增 user_settings 表（或 users 增加列）用于每用户推送时间与篇数；新增读写接口。
- 保持现有 papers/user_pushed 逻辑不变。

## 邮件推送
- 构造每用户 HTML 内容：基于其兴趣方向与设定篇数，按排序规则选取并渲染。
- SMTP 配置通过环境变量；失败安全处理不阻塞调度。

## Docker & 运行
- Dockerfile 复核：包含依赖安装、拷贝模板与代码、数据库迁移、非 root 用户运行；默认暴露 8000。
- 构建镜像并运行：映射 8000:8000；提供 docker-compose 选项（Open WebUI 保持独立 8080 端口）。
- Windows 11 注意事项：如遇拉取/解析问题，提供去除镜像源与设置 DNS 的指引；端口冲突时优先使用 8000 访问后端。

## 验证
- 容器启动后访问 / 与 /docs；注册→登录→选择兴趣→保存；管理员查看用户和设置；管理员界面分领域显示 10 篇；手动触发与定时推送（需 SMTP）均正常。

如确认以上方案，我将开始修改代码、更新模板与数据库接口，完成打包并在本机验证运行。